  // const addToCartReducer = (state, action) => {
  //   switch (action.type) {
  //     case "Add":
  //       return {
  //         ...state,
  //         // ...state.addItems,
  //         addItems:{
  //           ...state.addItems,
  //           addItems:action.payload
  //         },
  //         totalItems: Number(state.totalItems + 1), //+Number(state.temp),
  //       };
  //   }
  // };
  // const [state, dispatch] = useReducer(addToCartReducer, initialState);



      // const initialState = {
//   totalItems: 0,
//   temp: 0,
//   addItems: {},
// };

// code refernce for redux(reducer):

// const { state, dispatch } = useCart(); 

// dispatch({ payload: item,modifier: inputRef.current })

// const initialState={
//     totalItems:0,
//     itemRef:{},
//     // addItems:[{}],
//     // removeItems:[]
// }


    // const [state, dispatch] = useReducer(addToCartReducer, initialState);

    // value={ state, dispatch } 